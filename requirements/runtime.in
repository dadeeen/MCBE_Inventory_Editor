# Direct runtime dependencies. Keep these versions consistent with pyproject.toml.
# Compile lockfiles in the Python 3.12 target runtime with:
#   python scripts/compile_lockfiles.py
Flask==3.1.3
amulet-nbt==2.1.8
amulet-leveldb==1.0.6
colorama==0.4.6
